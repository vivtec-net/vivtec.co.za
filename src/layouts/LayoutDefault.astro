---
import BlockHead from "../blocks/BlockHead.astro";
import BlockNav from "../blocks/BlockNav.astro";
import BlockFoo from "../blocks/BlockFoo.astro";

const { title, description, image = "favicon.svg" } = Astro.props;
---

<!doctype html>
<html lang="en">
	<BlockHead title={title} description={description} image={image} />
	<body>
		<BlockNav />
		<main>
			<slot />
		</main>
		<BlockFoo />
	</body>
</html>

<style>
	body {
		display: grid;
		grid:
			"nav head" auto
			"nav main" 1fr
			"nav foo" auto / 200px 1fr;

		> nav {
			grid-area: nav;
		}
		> header {
			grid-area: head;
		}
		> main {
			grid-area: main;
		}
		> footer {
			grid-area: foo;
		}
	}
</style>
